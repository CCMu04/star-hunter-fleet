import { Card, Typography } from 'antd';

const { Title } = Typography;

const ModuleSlot: React.FC = () => {
  return (
    <Card>
      <Title level={3}>模块槽位</Title>
      <p>功能开发中...</p>
    </Card>
  );
};

export default ModuleSlot;
