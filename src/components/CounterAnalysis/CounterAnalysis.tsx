import { Card, Typography } from 'antd';

const { Title } = Typography;

const CounterAnalysis: React.FC = () => {
  return (
    <Card>
      <Title level={3}>克制分析</Title>
      <p>功能开发中...</p>
    </Card>
  );
};

export default CounterAnalysis;
