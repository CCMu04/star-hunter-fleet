import { Card, Typography } from 'antd';

const { Title } = Typography;

const FleetBuilder: React.FC = () => {
  return (
    <Card>
      <Title level={3}>舰队编组器</Title>
      <p>功能开发中...</p>
    </Card>
  );
};

export default FleetBuilder;
